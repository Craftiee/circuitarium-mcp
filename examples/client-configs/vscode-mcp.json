{
  "servers": {
    "circuitarium": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "circuitarium-mcp@0.3.0"],
      "env": {
        "CIRCUITARIUM_MCP_ROOT": "${workspaceFolder}"
      }
    }
  }
}
